[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run db:generate",
  "npm run build -w @iraq-clinic/clinic-system"
]

[start]
cmd = "node scripts/deploy-start.js"

[variables]
NODE_ENV = "production"
NEXT_OUTPUT = "standalone"
